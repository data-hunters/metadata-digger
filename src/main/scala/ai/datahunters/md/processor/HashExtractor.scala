package ai.datahunters.md.processor
import org.apache.spark.sql.DataFrame

class HashExtractor extends Processor {

  override def execute(inputDF: DataFrame): DataFrame = {

    inputDF

    return inputDF
  }
}
